## Dev-only fallback. The daemon injects window.__CARAPACE_TOKEN__ into the
## served page; this is only read by `npm run dev` against a daemon started
## separately. Copy to .env.local (gitignored) and fill in the token printed
## by / found in <state_dir>/api-token.
VITE_CARAPACE_TOKEN=
